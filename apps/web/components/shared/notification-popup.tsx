'use client'

/**
 * Notification popup stub for EduTrack admin.
 * Full realtime notifications require a `notifications` table to be set up.
 * Returns null until that infrastructure is in place.
 */
export function NotificationPopup() {
  return null
}
